import 'typeface-roboto';
import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
    primary: { main: '#0277BD' },
    secondary: { main: '#4DD0E1' }
};
const themeName = 'Lochmara';

export default createMuiTheme({ palette, themeName });
