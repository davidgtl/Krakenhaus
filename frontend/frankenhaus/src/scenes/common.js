import createMuiTheme from "@material-ui/core/styles/createMuiTheme";
import {amber, blue, orange, red} from "@material-ui/core/colors";

export const loginTheme = createMuiTheme({
    palette: {
        primary: blue,
        secondary: amber,
    },
});
